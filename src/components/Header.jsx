import React from 'react';
import { Container,Contai, Navbar } from 'react-bootstrap';

const Header = () => {
    return (
        <Container fluid className='text-center bg-warning font-weight-bolder'>
            <h2 style={{color:'green'}}>ToDo App</h2>
            </Container>
    );
}

export default Header;
